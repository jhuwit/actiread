% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acti_decompress_file.R
\name{acti_decompress_file}
\alias{acti_decompress_file}
\title{Decompress an Activity-data File}
\usage{
acti_decompress_file(path, extension = NULL)
}
\arguments{
\item{path}{Path to an activity-data file, optionally compressed.}

\item{extension}{Output file extension. Defaults to \code{NULL}, which infers the
extension from \code{path} after removing its compression extension.}
}
\value{
A list with \code{path}, the usable file path, and \code{temporary}, which is
\code{TRUE} when a temporary decompressed file was created. Callers should
delete \code{path} when \code{temporary} is \code{TRUE}.
}
\description{
Decompresses \code{.bz2}, \code{.gz}, and \code{.xz} files to a temporary file. If
\code{extension} is \code{NULL}, the destination extension is inferred after removing
the compression extension from \code{path}.
}
\examples{
file = acti_decompress_file(acti_example_cwa())
if (file$temporary) {
  unlink(file$path)
}
}
